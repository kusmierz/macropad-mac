## What

<!-- The change itself, in a few lines or bullets. Behaviour, not a file-by-file diff narration. -->

## Why

<!-- The problem this solves: the bug, the gap, the symptom someone reported. Link the PR that
introduced a regression (#NNN), the ticket, or the log line / measurement that made the case. -->

## How

<!-- Only the mechanics a reviewer can't get from the diff: where the fix lands and why there,
guards and their order, anything deliberately left out of scope. Delete this section if the diff
speaks for itself. -->

## Tests

<!-- What was added or run, and the result. Include the pass count and whether type-check/lint
are clean. Note any manual verification (dry run, staging, prod logs). -->
